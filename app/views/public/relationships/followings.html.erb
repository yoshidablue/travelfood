<div class="container">
  <div class="row mt-5">
    <h4>フォロー一覧</h4>
  </div>

  <% if @customers.exists? %>
    <%= render 'public/customers/index', customers: @customers %>
  <% else %>
    <p>ユーザーはいません</p>
  <% end %>

</div>